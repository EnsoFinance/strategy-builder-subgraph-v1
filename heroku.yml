setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    web: Dockerfile.web
    graph: Dockerfile.graph
    ipfs: Dockerfile.ipfs
release:
  image: web
  command:
    - npm run build:remote && npm run deploy:local
