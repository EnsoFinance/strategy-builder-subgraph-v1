setup:
  addons:
    - plan: heroku-postgresql
      as: DATABASE
build:
  docker:
    graph: Dockerfile.graph
    ipfs: Dockerfile.ipfs
release:
  image: graph
  command:
    - yarn && yarn build:remote && yarn deploy:local
