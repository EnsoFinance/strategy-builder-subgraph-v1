apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: enso-subgraph
  name: enso-subgraph
spec:
  replicas: 1
  selector:
    matchLabels:
      app: enso-subgraph
  template:
    metadata:
      labels:
        app: enso-subgraph
    spec:
      containers:
      - image: ensoassociation/deploy-enso-subgraph
        env:
          - name: GRAPH_ADMIN_URL
            value: http://graph-node.graph-node:8020
          - name: IPFS_URL
            value: http://ipfs.ipfs:5001
        name: deploy-enso-subgraph
      imagePullSecrets:
        - name: regcred
